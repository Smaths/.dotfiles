# OS metadata
.DS_Store
Icon?

# Editor/IDE
.vscode/
.idea/

# Common temp files
*.log
*.pid
*.seed
*.swp
*.swo

# Local-only zsh overrides (template is tracked)
config/zsh/local.zsh

# Local-only bash overrides (template is tracked)
config/bash/local.bash

# Zsh runtime artifacts (if generated in repo)
.zcompdump*
.zsh_sessions/
.zsh_history
.zsh-plugins/

# Codex local/runtime artifacts (keep project config tracked)
.codex/*
!.codex/config.toml
AGENTS.md
