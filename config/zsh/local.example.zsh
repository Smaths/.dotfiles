# Copy this file to local.zsh for machine-specific, non-committed overrides.
# Example:
# export EDITOR="nvim"
# alias workvpn='open -a "My VPN"'

# Example app/tool machine-specific overrides (keep these local, not in tracked .zshrc/.zprofile).
# export PATH="$PATH:/Applications/Obsidian.app/Contents/MacOS"
# fpath=("$HOME/.docker/completions" $fpath)
# autoload -Uz compinit && compinit


# Collidascope overrides
# export CSK_VPS_USER="eric"
# export CSK_VPS_HOST="5.161.181.25"
# export CSK_REPO_ROOT="$HOME/Developer/CollidascopeVR/CollidascopeStudio"
# export LINUX_SERVER_BUILD_DIR="$CSK_REPO_ROOT/Builds/ServerLinux64"
